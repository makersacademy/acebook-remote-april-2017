<!DOCTYPE html>
<html>
  <body>
    <h1>Photos</h1>
    <%= button_to new_photo_path do%>
    New photo
    <%end%>
    <br>
    <br>

    <% @photos.each do |photo| %>
    <% @commentable = photo%>
      <%= @commentable.title %><br>
      <%= image_tag @commentable.image.url (:medium)%> <br>
      <h2> Comments</h2>
      <%= render @commentable.comments %>
      <h2> Add a comment: </h2>
      <%= render 'comments/photo_form' %>
    <% end %>
  </body>
</html>
